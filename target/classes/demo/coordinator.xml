<coordinator-app name="hello-coord" 
	frequency="${coord:minutes(1)}"  
	start="${start}" 
	end="${end}" 
	timezone="UTC"
	xmlns="uri:oozie:coordinator:0.1">
      <action>
        <workflow>
          <app-path>${workflowAppUri}</app-path>
       </workflow>
      </action>	
</coordinator-app>